---
title: "Regression Visualization"
author: "Kate Houston"
format: 
  html:
    self-contained: True
editor: visual
---

## Multivariate Regression Model Visualization

```{r}
library(tidyverse)
library(readr)
```

```{r data}
data <- read_csv("cleaned_full_class_data.csv", show_col_types = FALSE)
data
```

```{r data results}
results <- read_csv("regression_results.csv", show_col_types = FALSE)
results
```

```{r intercept}
intercept <- read_csv("regression_intercept.csv", show_col_types = FALSE)
intercept
```

```{r regression plot}
data <-
  ggplot(
    aes(x = ..., y = ...)
  )
```
